# Azure using terraform

this folder contains some basic infra setup using terraform on the azure cloud. 

VNET->subnet->NIC(network interface card)+VM(ssh and public ip attatched), also create a b;lob to store the state file remotely

